import React from 'react'
import { Outlet } from 'react-router'

export const IniciarSesion = () => {
    return (
        <div>
            desde iniciar sesion
            <Outlet/>
        </div>
    )
}
